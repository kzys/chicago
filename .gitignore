# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Generated by scripts/provision_wifi_json.py (WiFi passwords) or hand-built
# for install-baseten-state (API keys) -- real secrets, never committed.
# secrets.py itself has no embedded secrets anymore (see badge/apps/config
# and the bottom of secrets.py) so it's fine to track.
config.json
baseten.json
